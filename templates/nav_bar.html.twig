<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm sticky-top">
    <div class="container">
        <a class="navbar-brand fw-bold" href="{{ path('app_home') }}">
            WorkTogether
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMain" aria-controls="navbarMain" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarMain">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_home') }}#offres">Nos offres</a>
                </li>
            </ul>

            {# Les boutons à droite #}
            <div class="d-flex align-items-center">

                {% if app.user %}
                    {# Si l'utilisateur EST connecté #}
                    <span class="text-light me-3">
                        Bonjour, <strong>{{ app.user.userIdentifier }}</strong>
                    </span>

                    <a href="{{ path('app_cart') }}" class="btn btn-sm btn-outline-warning me-2">Mon Panier</a>

                    <a href="{{ path('app_customer') }}" class="btn btn-sm btn-outline-info me-2">Mon Espace</a>

                    <a href="{{ path('app_logout') }}" class="btn btn-sm btn-outline-danger">Déconnexion</a>
                {% else %}
                    {# Si l'utilisateur N'EST PAS connecté #}
                    <a href="{{ path('app_cart') }}" class="btn btn-sm btn-outline-warning me-2">Mon Panier</a>
                    <a href="{{ path('app_login') }}" class="btn btn-sm btn-outline-light me-2">Connexion</a>
                    <a href="{{ path('app_register') }}" class="btn btn-sm btn-outline-primary">Inscription</a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>
